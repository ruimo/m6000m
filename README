--- METEX M-6000M data receiver ---

Prerequisite:

- Serialport library
Need to install serialport library(gem install serialport).

- Ruby
Tested with 1.8.7

How to run:

./m6000m.rb <device name>

or

ruby m6000m.rb <device name>

The device name is such as /dev/ttyUSB0 (Linux) or COM6 (Windows).
